#include<stdio.h>
#include<conio.h>
int main (void)
{
    int a, i, sum=0;
   do
   {
     printf("Enter number");
        scanf("%d:", &a);
           printf("%d:", sum);
            sum=sum+a;
        while(a<0);
            break;
    }
}